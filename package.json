{
  "name": "pacs008.github.io",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "./build.sh --serve",
    "serve": "python3 -m http.server 8000 --directory public --bind 127.0.0.1",
    "build": "./build.sh",
    "check": "ssg check -f config.toml",
    "audit": "ssg audit -f config.toml",
    "validate": "html-validate 'public/**/*.html'",
    "test": "ssg check -f config.toml"
  },
  "devDependencies": {
    "@axe-core/cli": "4.11.1",
    "@vue/test-utils": "2.4.6",
    "happy-dom": "20.8.8",
    "html-validate": "10.11.2",
    "serve": "14.2.6",
    "vitepress": "^1.6.3",
    "vitest": "4.1.1"
  },
  "overrides": {
    "esbuild": ">=0.25.0"
  }
}
